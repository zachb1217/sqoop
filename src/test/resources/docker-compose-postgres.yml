version: '3'
services:
  postgres:
    image: postgres
    environment:
        POSTGRES_DB: metastore
        POSTGRES_USER: root
        POSTGRES_PASSWORD: example
    ports:
      - 5432
    healthcheck:
      test: ["CMD-SHELL", "pg_isready"]
      timeout: 10s
      retries: 5
